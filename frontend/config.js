// MRIT Hub Frontend Configuration
const CONFIG = {
    API_BASE_URL: '/api/v1',  // Use relative path for nginx proxy
    // API_BASE_URL: 'http://localhost:3000/api/v1',  // Use this for direct backend access
};
